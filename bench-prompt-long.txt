Read the following technical passage carefully and then answer the question at the end.

The memory subsystem of a modern accelerator is organised as a hierarchy in which each level trades capacity against latency and bandwidth. When a kernel streams weights from device memory it is limited by the achievable bandwidth rather than the peak arithmetic rate, and the ratio between the two determines whether the kernel is compute-bound or memory-bound. A mixture-of-experts layer complicates this analysis because the set of weights touched depends on the routing decision for each token, so the effective working set grows with the batch until the union of selected experts saturates. Understanding this behaviour is essential when reasoning about speculative decoding, where a batch of candidate tokens is verified in a single forward pass. The memory subsystem of a modern accelerator is organised as a hierarchy in which each level trades capacity against latency and bandwidth. When a kernel streams weights from device memory it is limited by the achievable bandwidth rather than the peak arithmetic rate, and the ratio between the two determines whether the kernel is compute-bound or memory-bound. A mixture-of-experts layer complicates this analysis because the set of weights touched depends on the routing decision for each token, so the effective working set grows with the batch until the union of selected experts saturates. Understanding this behaviour is essential when reasoning about speculative decoding, where a batch of candidate tokens is verified in a single forward pass. The memory subsystem of a modern accelerator is organised as a hierarchy in which each level trades capacity against latency and bandwidth. When a kernel streams weights from device memory it is limited by the achievable bandwidth rather than the peak arithmetic rate, and the ratio between the two determines whether the kernel is compute-bound or memory-bound. A mixture-of-experts layer complicates this analysis because the set of weights touched depends on the routing decision for each token, so the effective working set grows with the batch until the union of selected experts saturates. Understanding this behaviour is essential when reasoning about speculative decoding, where a batch of candidate tokens is verified in a single forward pass. The memory subsystem of a modern accelerator is organised as a hierarchy in which each level trades capacity against latency and bandwidth. When a kernel streams weights from device memory it is limited by the achievable bandwidth rather than the peak arithmetic rate, and the ratio between the two determines whether the kernel is compute-bound or memory-bound. A mixture-of-experts layer complicates this analysis because the set of weights touched depends on the routing decision for each token, so the effective working set grows with the batch until the union of selected experts saturates. Understanding this behaviour is essential when reasoning about speculative decoding, where a batch of candidate tokens is verified in a single forward pass. The memory subsystem of a modern accelerator is organised as a hierarchy in which each level trades capacity against latency and bandwidth. When a kernel streams weights from device memory it is limited by the achievable bandwidth rather than the peak arithmetic rate, and the ratio between the two determines whether the kernel is compute-bound or memory-bound. A mixture-of-experts layer complicates this analysis because the set of weights touched depends on the routing decision for each token, so the effective working set grows with the batch until the union of selected experts saturates. Understanding this behaviour is essential when reasoning about speculative decoding, where a batch of candidate tokens is verified in a single forward pass. The memory subsystem of a modern accelerator is organised as a hierarchy in which each level trades capacity against latency and bandwidth. When a kernel streams weights from device memory it is limited by the achievable bandwidth rather than the peak arithmetic rate, and the ratio between the two determines whether the kernel is compute-bound or memory-bound. A mixture-of-experts layer complicates this analysis because the set of weights touched depends on the routing decision for each token, so the effective working set grows with the batch until the union of selected experts saturates. Understanding this behaviour is essential when reasoning about speculative decoding, where a batch of candidate tokens is verified in a single forward pass. The memory subsystem of a modern accelerator is organised as a hierarchy in which each level trades capacity against latency and bandwidth. When a kernel streams weights from device memory it is limited by the achievable bandwidth rather than the peak arithmetic rate, and the ratio between the two determines whether the kernel is compute-bound or memory-bound. A mixture-of-experts layer complicates this analysis because the set of weights touched depends on the routing decision for each token, so the effective working set grows with the batch until the union of selected experts saturates. Understanding this behaviour is essential when reasoning about speculative decoding, where a batch of candidate tokens is verified in a single forward pass. The memory subsystem of a modern accelerator is organised as a hierarchy in which each level trades capacity against latency and bandwidth. When a kernel streams weights from device memory it is limited by the achievable bandwidth rather than the peak arithmetic rate, and the ratio between the two determines whether the kernel is compute-bound or memory-bound. A mixture-of-experts layer complicates this analysis because the set of weights touched depends on the routing decision for each token, so the effective working set grows with the batch until the union of selected experts saturates. Understanding this behaviour is essential when reasoning about speculative decoding, where a batch of candidate tokens is verified in a single forward pass. 

Question: In two sentences, explain why the effective working set of a mixture-of-experts layer grows with batch size, and what limits that growth.
